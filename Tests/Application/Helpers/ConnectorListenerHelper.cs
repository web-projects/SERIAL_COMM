namespace DEVICE_CORE.Helpers.Tests
{
    //public class ConnectorListenerHelper : IListenerConnector
    public class ConnectorListenerHelper
    {
        /*
        public AppType CurrentAppType => throw new NotImplementedException();

        public HashSet<string> CurrentTopics => throw new NotImplementedException();

        public IDictionary<string, object> CurrentAttributes => throw new NotImplementedException();

        public string DefaultCustomerIdentifier => throw new NotImplementedException();

        public IChannelClient ChannelClient => throw new NotImplementedException();

        public event OnMessageReceivedHandler MessageReceived;
        public event OnMessageSendConfirmHandler MessageSentConfirmed;
        public event OnMessageSendFailedHandler MessageSentFailed;
        public event OnOfflineConnectivityHandler OfflineConnectivity;
        public event OnOnlineConnectivityHandler OnlineConnectivity;
        public event OnChannelErrorEventHandler ChannelErrorOccured;
        public event OnChannelConnected ChannelConnected;
        public event Listener.Connector.OnChannelDisconnected ChannelDisconnected;

        public object PublishedMessage { get; private set; }

        public Task<bool> Connect()
        {
            return Task.FromResult(true);
        }

        public void Dispose()
        {

        }

        public Task Publish(object message, params string[] topics)
        {
            return Task.CompletedTask;
        }

        public Task Publish(object message, params TopicOption[] topics)
        {
            PublishedMessage = message;
            return Task.CompletedTask;
        }

        public Task Publish(object message, IDictionary<string, string> attributes, params string[] topics)
        {
            return Task.CompletedTask;
        }

        public IListenerConnector SetDefaultCustomerIdentifier(string customerIdentifier)
        {
            return null;
        }

        public Task Subscribe(params string[] topics)
        {
            return Task.CompletedTask;
        }

        public Task Subscribe(params TopicOption[] topics)
        {
            return Task.CompletedTask;
        }

        public Task Subscribe(IDictionary<string, string> attributes, params string[] topics)
        {
            return Task.CompletedTask;
        }

        public Task Unsubscribe()
        {
            return Task.CompletedTask;
        }

        public Task Unsubscribe(params string[] topics)
        {
            return Task.CompletedTask;
        }

        public Task Unsubscribe(params TopicOption[] topics)
        {
            return Task.CompletedTask;
        }
        */
    }
}
